{
  "_note": "backend URL below is duplicated in the repo-root vercel.json — keep both in sync if the Cloud Run service URL ever changes",
  "rewrites":[
    {"source":"/api/:path*", "destination":"https://pashxd-api-214145020309.europe-west1.run.app/api/:path*"},
    {"source":"/(.*)", "destination":"/index.html"}
  ]
}
