# Backend API base URL.
# Local dev:   http://127.0.0.1:8000
# Production:  the Cloud Run service URL (or https://api.pashx.com once
#              the custom domain is mapped). Set this in Vercel too.
VITE_API_URL=http://127.0.0.1:8000
